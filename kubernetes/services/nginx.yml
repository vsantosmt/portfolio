apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080  # Porta acessível no host
  - protocol: TCP
    port: 443
    targetPort: 443
    nodePort: 30443
  type: NodePort
